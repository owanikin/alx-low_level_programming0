#include <stdio.h>

/**
 * main - prints alphabet in lower letter
 * Return: Always 0
 */

int main(void)
{
	char lc;

	for (lc = 'a'; lc <= '2'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
